/*
规则网格和交错网格PML对比：规则网格
*/
#include "cstd.h"
float *ricker(int nt, float dt, float fm, float t0);
int main(int argc, char **argv)
{
    initargs(argc, argv);
    int nz, nx;
    float dz, dx;

    return 0;
}